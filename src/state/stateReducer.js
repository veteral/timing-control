export const stateReducer = (state, action) => {
    switch (action.type) {
      case GET_DOCUMENTS:
        return ({
          ...data
        });
      
      console.log('data reduce', data);
      default: return state
    }
  }
  